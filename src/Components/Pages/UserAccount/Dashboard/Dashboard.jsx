import { Helmet } from "react-helmet-async";

const Dashboard = () => {
    return (
        <section>
            <Helmet>
                <title>G-Shop | Dashboard</title>
            </Helmet>
        </section>
    );
};

export default Dashboard;