import { Helmet } from "react-helmet-async";

const Orders = () => {
    return (
        <section>
            <Helmet>
                <title>G-Shop | Orders</title>
            </Helmet>
        </section>
    );
};

export default Orders;